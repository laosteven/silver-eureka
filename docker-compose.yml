services:
  trivia-n-chill:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - CONFIG_PATH=/config/game.yml
      - GAME_TITLE=${GAME_TITLE:-Trivia & Chill}
    volumes:
      - ./config:/config:ro
    restart: unless-stopped
